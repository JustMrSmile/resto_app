<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:RestoApp.Desktop"
             xmlns:vm="using:RestoApp.Desktop.ViewModels"
             xmlns:v="using:RestoApp.Desktop.Views"
             x:Class="RestoApp.Desktop.App"
             RequestedThemeVariant="Light">

    <Application.DataTemplates>
        <local:ViewLocator/>

        <!-- Mapeos de ViewModel a Vista para todas las secciones -->
        <DataTemplate DataType="{x:Type vm:InicioViewModel}">
            <v:InicioView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MesasViewModel}">
            <v:MesasView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ReservasViewModel}">
            <v:ReservasView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:EmpleadosViewModel}">
            <v:EmpleadosView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CajaViewModel}">
            <v:CajaView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:EventosViewModel}">
            <v:EventosView/>
        </DataTemplate>
    </Application.DataTemplates>

    <Application.Styles>
        <FluentTheme/>
        <StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml"/>
    </Application.Styles>
</Application>